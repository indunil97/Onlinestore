import 'package:flutter/material.dart';

class InvalidPassword extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text("errror", style: TextStyle(color: Colors.red));
  }
}